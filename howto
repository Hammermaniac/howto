#!/bin/bash
echo ''

n=$#

for n do
  grep -i "$1" ~/.common-configs/howtos | while read -r line
    do
  	  #echo  ""
  	  line=$(echo -e "$line" | sed 's/: /:\n/')
	  echo -e "$line\n"
    done
  shift
done
